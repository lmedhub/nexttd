import { Typography } from "@mui/material";

export default function DashBoard() {
  //   const { data: cats, isLoading } = useQuery(catsFactsQuery());

  return (
    <>
      <Typography>Olá - Dashboard</Typography>
    </>
  );
}
